<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.com.yourHelper.Dao.ArticleRepository">

	<insert id="saveArticle">
		INSERT INTO tb_article (member_id, category_id, title, create_date, hit)
		VALUES (#{memberId}, #{categoryId}, #{title}, now(), 0)
		
		<selectKey resultType="String" keyProperty="articleId" order="AFTER">
        	SELECT LAST_INSERT_ID()
    	</selectKey>
	</insert>
	
	<insert id="saveContent">
		INSERT INTO tb_article_content (article_id, content, create_date)
		VALUES (#{articleId}, #{content}, now())
	</insert>
	
	<select id="findCategoryByCode" resultType="kr.com.yourHelper.Dto.CategoryDto">
		SELECT *
		FROM tb_category
		WHERE code = #{code}
	</select>
	
</mapper>