<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.com.yourHelper.Dao.MemberRepository">
	
	<insert id="save">
		INSERT INTO tb_member (member_id, password, nick_name, create_date)
		VALUES (#{memberId}, #{password}, #{nickName}, now())
	</insert>
	
</mapper>